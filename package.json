{
  "name": "bcvision-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix servers\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
